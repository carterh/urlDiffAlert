import url_diff
import json

def main():
    #Load state

    #Load config

    #Create new state entries for config entries that don't already exist (or if the regex is different)

    #while true:
    #Every X minutes, run url_diff for every key in the state dictionary
    #If no change, continue
    #Else:
    #send email alert, update state, write the cache to disk
    pass

if __name__ == '__main__':
    main()
